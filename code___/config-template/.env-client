HOSTING=DOCKER
NODE_ENV=development

// DON't FORGET TO UPDATE docker-compose file [ port-binding ]
VUE_APP_PORT=80
VUE_APP_API_PORT=4000

// Use  localhost       if working on localhost
// Use  local ip        if working on local network
// Use  public ip       if working over internet
VUE_APP_API_HOST=localhost

VUE_APP_API_HOST_DOCKER=nodejs-server
